_A helper function to generate a special StyledComponent that creates global styles._
_Place it at the top of your React tree and the global styles will be injected when the component is "rendered"._

Example:

```jsx static
<GlobalStyle />
```

---
