// @flow

// import type { Props as ShapeProps } from "./shape";

// import React from "react";
// import styled, { css } from "styled-components";
// import Shape, { tagsMapping } from "./shape";

// type Props = ShapeProps & {
//   children: React$Node
// };

// const Heading = ({ children, size, ...rest }: Props) => (
//   <Shape as={tagsMapping[size]} size={size} {...rest}>
//     {children}
//   </Shape>
// );

export default null;
